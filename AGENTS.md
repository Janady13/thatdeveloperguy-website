# thatdeveloperguy-website

- For any Rive work (RML, `rive` CLI, `.riv`/`.rev`, state machines, data binding, runtime integration) use the `rive-expert` skill (`.agents/skills/rive-expert/SKILL.md`). The installed Rive CLI, `rive schema`, `rive docs` and the current Rive documentation are authoritative over memory.
- Rive CLI projects live in `rive/<room>/`; each has its own Rive-generated `AGENTS.md` — follow it.
- Verification is evidence: `--verify`, `inspect`, `--screenshot`, driven interaction; never claim a check that did not run.
