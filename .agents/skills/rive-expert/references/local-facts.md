# Verified facts for this machine and repo (2026-09-16)

- CLI: `rive 1.0.4` at `~/.rive/bin/rive` (`export PATH=$HOME/.rive/bin:$PATH`). `rive doctor` ok; not logged in (needed only for `--publish`, `--rev`, `push`).
- **`rive docs` shadowing trap**: it resolves topics against a `./docs` folder if one exists in the cwd. `~/docs/superpowers/…` exists, so from `~` every topic is "not found" and `--list` shows the wrong files. Run `rive docs` from inside the Rive project directory. The bundled reference is `~/.rive/versions/<version>/docs/*.md` (format, skeleton, assets, drawing, text, rigging, transforms, easing, layout, state-machines, data, semantics, cursor, focus, debugging, workflow, publishing, push, gotchas, luau/, project/rive-yaml.md).
- Editor: `/Applications/Rive Early Access.app` exposes MCP 0.6 at `http://127.0.0.1:9791/mcp` only while running; tools need a file OPEN in the editor (`session_info.activeFileId`). The `app.rive.ea-editor://` URL scheme does not open cloud files; the owner opens `editor.rive.app/file/thatdeveloperguy-website/2583640` by hand. JSON-RPC client: `tools/rive-mcp.ts` (needs `notifications/initialized`). MCP `export_file` excludes MCP-created artboards; `upload_asset` takes data URIs; `inline_base64` export ≤ 8 MiB.
- Node for repo scripts: `~/.local/node/node-v24.21.0-darwin-arm64/bin` (`node --import tsx …`; `--experimental-strip-types` fails on parameter properties, use `--experimental-transform-types`).
- Runtime deps in package.json: `@rive-app/canvas-advanced` 2.42.1, `@rive-app/react-webgl2` 4.34.2, React 19, React Router 8 (ssr:false + prerender).
- Rive CLI projects in this repo: `rive/<room>/` (tracked). Earlier experiment `.tools/rive-projects/lobby-layers` (git-ignored) placed the 60 raw 1× separation layers — superseded.
- Image assets: `Image` has no width/height (asset pixel size); `originX/originY` default 0.5; place at `x,y` with `scaleX/scaleY` to map 2× plates onto a 1× artboard. `ImageAsset file=` embeds bytes; omit `file` to ship contentless and supply at runtime via the asset loader.
- Plates pipeline (`creative-source/plates/lobby/`): Cloudinary `e_upscale` (4×, tile 2×2 for the 10 MB cap), `e_gen_remove` output composited only inside layer masks, OpenCV inpaint for lettering on flat plates, layers cut from the 4× master, WebP 2× (0.63 MB) and 4× (1.52 MB) sets, manifests in source-pixel coords (artboard 1672×941).
- Staging: `https://tdg.thatwebhostingguy.com/` root `/var/www/sites/tdg` on Bubbles; CSP `script-src 'self'` — inline scripts silently no-op; ship JS as files.
