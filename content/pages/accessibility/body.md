**Tested behaviour.** Every room door is a real link reachable with the keyboard; a visible focus ring marks it. The caption under the room names whatever is focused. Pages are complete without JavaScript and without the animation. `prefers-reduced-motion` turns ambient motion off and makes door transitions immediate.

**Automated checks** run axe-core on every published route in Chromium, WebKit and a mobile profile. **Manual review** with a screen reader is recorded separately and is not claimed until it has been done.
